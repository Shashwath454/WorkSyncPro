<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= user.name %> - Salary Calculator</title>
  <style>
    body {
      background-color: #121212;
      color: #fff;
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .calculator-container {
      background-color: #1e1e1e;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
      text-align: center;
    }

    h1 {
      color: #f0f0f0;
    }

    .result {
      margin-top: 20px;
      font-size: 18px;
    }
  </style>
</head>

<body>
  <div class="calculator-container">
    <h1>Salary Calculator for <%= user.name %></h1>
    <p>Position: <%= user.position %></p>
    <p>Monthly Salary: $<%= user.monthlySalary %></p>
    <div class="result">
      <p><strong>Annual Salary:</strong> $<%= user.monthlySalary * 12 %></p>
      <!-- You can add more calculations or information as needed -->
    </div>
  </div>
</body>

</html>
